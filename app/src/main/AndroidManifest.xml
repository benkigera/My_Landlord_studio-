<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.MyLandlordStudio">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PropertyListView"></activity>
        <activity android:name=".Unit" />
        <activity android:name=".PropertyActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".Tenant_details_activity" />
        <activity android:name=".Monthly_reports_view_activity" />
        <activity android:name=".ApplicationFrame" />
        <activity android:name=".RegisterActivity" />

        <activity android:name=".LoginAcitivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>